<div class="row mt-4">
  <div class="col-md-4">
    <div class="list-group entity-type-list">
      {{#each model as |department|}}
        <div class="list-group-item">
          {{department.dname}}, {{department.loc}}
        </div>
      {{/each}} 
    </div>  
  </div>

  <div class="col-md-8">
    {{outlet}}
  </div>

</div>
